// Placeholder — generate the real file with admin/setup.html and replace this.
// It will contain the repo owner/name and the GitHub token encrypted
// with the admin password (AES-256-GCM, PBKDF2 key derivation).
window.MC_CONFIG = null;
